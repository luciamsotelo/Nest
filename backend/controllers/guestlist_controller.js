// const { Guestlist } = require('../models');

// const guestlistController = {
//   getAllGuestslists: async (req, res) => {
//     try {
//       const guestlists = await Guestlist.findAll();
//       res.json(guestlists);
//     } catch (error) {
//       console.error(error);
//       res.status(500).json({ message: 'Internal server error' });
//     }
//   },
//   // Implement other controller functions for CRUD operations
// };

// module.exports = guestlistController;

